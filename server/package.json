{
  "name": "server",
  "version": "1.0.0",
  "description": "MERN Authentication API",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "cd ../client && npm install && npm run build",
    "vercel-build": "npm install && npm run build && cd ../client && npm install && npm run build",
    "render-build": "chmod +x ./render-build.sh && ./render-build.sh"
  },
  "engines": {
    "node": ">=14.0.0"
  },
  "license": "MIT",
  "dependencies": {
    "bcryptjs": "2.4.3",
    "cookie-parser": "^1.4.7",
    "cors": "2.8.5",
    "dotenv": "16.0.3",
    "express": "^4.21.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^6.13.8",
    "nodemailer": "^6.10.1",
    "nodemon": "^3.1.10",
    "path-to-regexp": "^6.3.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
